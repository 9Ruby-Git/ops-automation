# IXR Command Bus

Ruby (cloud) → writes tasks here → Claude Code/Gravity picks up and executes.

## Directories
- `queue/` — pending tasks
- `done/` — completed tasks  
- `failed/` — failed tasks

See `bot-council/task_bus.md` for full spec.
